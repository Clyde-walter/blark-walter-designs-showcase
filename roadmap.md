# Roadmap

- [x] Add real live-site previews to project detail pages with `liveUrl` values.
- [ ] Turn every project detail route into a complete, readable standalone case study.
- [ ] Replace the shared hero portrait across the public site with the newly attached photo.
- [ ] Restyle the admin panel to match the user's reference image. Blocked until the image is reattached.